--extra-index-url https://download.pytorch.org/whl/cu121

# core DL
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121

# numerical stack (numpy<2 is critical)
numpy==1.26.4
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
pillow>=10.0
opencv-python>=4.8
pyyaml>=6.0
tqdm>=4.66
psutil
py-cpuinfo
requests
scikit-learn>=1.3

# ultralytics runtime deps (the repo's setup.py asks for these too,
# listing them here makes resolution explicit)
ultralytics-thop

# ONNX (optional, drop if unused)
onnx>=1.15
onnxruntime-gpu==1.17.1

# Jupyter
jupyterlab
ipywidgets
ipykernel

# misc
roboflow